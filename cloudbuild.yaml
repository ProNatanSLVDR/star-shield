steps:
  - id: build-docker-image
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/prod-builds/starshield-app:latest'
      - '-t'
      - 'starshield-app'
      - '-f'
      - './Dockerfile'
      - '.'

  - id: extract-static-files
    name: gcr.io/cloud-builders/docker
    entrypoint: bash
    args:
      - '-c'
      - |
        docker create --name static_helper starshield-app
        docker cp static_helper:/app/staticfiles .
        docker rm static_helper
    waitFor: ['build-docker-image']

  - id: push-static-files-to-bucket
    name: gcr.io/cloud-builders/gsutil
    args:
      - "-m"
      - "rsync"
      - "-r"
      - "-d"
      - "./staticfiles/"
      - "gs://starshield-static"
    waitFor: ['extract-static-files']

  - id: push-image-to-artifact-registry
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/prod-builds/starshield-app:latest'
    waitFor: ['build-docker-image']

  - id: deploy-to-cloud-run-webapp
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: 
      - 'run'
      - 'deploy'
      - 'web-app'
      - '--image'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/prod-builds/starshield-app:latest'
      - '--region'
      - '${_REGION}'
    waitFor: ['push-image-to-artifact-registry']
    
  - id: deploy-to-cloud-run-tasks-api
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: 
      - 'run'
      - 'deploy'
      - 'tasks-api'
      - '--image'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/prod-builds/starshield-app:latest'
      - '--region'
      - '${_REGION}'
    waitFor: ['push-image-to-artifact-registry']

  - id: execute-migrations-job
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'jobs'
      - 'update'
      - 'migrate-prod'
      - '--image=${_REGION}-docker.pkg.dev/$PROJECT_ID/prod-builds/starshield-app:latest'
      - '--region=${_REGION}'
      - '--execute-now'
      - '--wait'
    waitFor: ['push-image-to-artifact-registry']

options:
  logging: CLOUD_LOGGING_ONLY
